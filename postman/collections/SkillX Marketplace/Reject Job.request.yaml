$kind: http-request
method: POST
url: '{{baseUrl}}/job/{{jobId}}/reject'
order: 10000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: '{"freelancer_wallet":"{{freelancerWallet}}"}'
